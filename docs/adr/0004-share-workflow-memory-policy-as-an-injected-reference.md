# Share workflow memory policy as an injected reference

Pair, implement, architecture, and teach need one authority, recall, capture, correction, failure, and flush policy but different lifecycle placement. Put the common policy in an installed `shared/memory` reference and keep hook timing in each skill; a dedicated callable memory skill was rejected because nested skill invocation would add routing and context overhead and would hide authority decisions behind a broader runtime boundary.
